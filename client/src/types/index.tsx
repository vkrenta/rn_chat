import {ReactNode} from 'react';

export type TRoundedInput = {
  children?: ReactNode;
  placeholder?: string;
  secure?: boolean;
  icon?: any;
};

export type TFormButton = {
  children?: ReactNode;
};
